(function() {
    'use strict';

    angular
        .module('utubelike')
        .controller('ChannelController', ChannelController);

    /** @ngInject */
    function ChannelController(MainService, ngProgressFactory) {
    	var vm = this;

    }
})();